{
  "name": "agent-context-compressor",
  "description": "Tool for semantically compressing agent conversation history into a minimal context window using LLM summarization",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "build": "tsc"
  },
  "dependencies": {
    "commander": "^11.1.0"
  },
  "devDependencies": {
    "bun-types": "^1.0.0",
    "typescript": "^5.3.0"
  }
}